spring:
  application:
    name: policyservice

server:
  port: 8001
---

---
# dev
spring:
    profiles:
        active: dev

    # mongo
    mongodb:
      uri: mongodb+srv://d_mongo_kit:TempPwd@policymanagementsystem.oxemfpl.mongodb.net/PolicyManagementSystem

---
# qa
spring:
    profiles:
        active: qa

    # mongo
    mongodb:
      uri: mongodb+srv://d_mongo_kit:TempPwd@policymanagementsystem.oxemfpl.mongodb.net/PolicyManagementSystem

---
# prod
spring:
    profiles:
        active: prod

    mongodb:
      uri: mongodb+srv://d_mongo_kit:TempPwd@policymanagementsystem.oxemfpl.mongodb.net/PolicyManagementSystem

